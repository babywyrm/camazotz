apiVersion: apps/v1
kind: Deployment
metadata:
  name: brain-gateway
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "camazotz.selectorLabels" "brain-gateway" | nindent 4 }}
    {{- include "camazotz.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.gateway.replicas }}
  selector:
    matchLabels:
      {{- include "camazotz.selectorLabels" "brain-gateway" | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "camazotz.selectorLabels" "brain-gateway" | nindent 8 }}
    spec:
      volumes:
        - name: flags
          emptyDir: {}
        {{- if .Values.nullfield.enabled }}
        - name: nullfield-tools
          configMap:
            name: nullfield-tools
        {{- if .Values.nullfield.activePolicySource }}
        # Controller-managed bridged ConfigMap (typically nullfield-controller's
        # syncActivePolicy). Mounted at its own path so the chart's
        # /etc/nullfield/* stays intact; sidecar's NULLFIELD_POLICY_PATH env
        # points the loader at this overlay path instead.
        - name: nullfield-active-policy
          configMap:
            name: {{ .Values.nullfield.activePolicySource | quote }}
            optional: true
        {{- end }}
        {{- end }}
      containers:
        - name: brain-gateway
          image: "{{ .Values.gateway.image }}:{{ .Values.gateway.tag }}"
          imagePullPolicy: {{ .Values.gateway.pullPolicy }}
          ports:
            - containerPort: {{ .Values.gateway.port }}
          envFrom:
            - configMapRef:
                name: camazotz-config
            - secretRef:
                name: camazotz-secrets
          env:
            - name: CAMAZOTZ_FLAGS_DIR
              value: {{ .Values.gateway.flagsDir | quote }}
            - name: CAMAZOTZ_MODULES_DIR
              value: {{ .Values.gateway.modulesDir | default "/workspace/camazotz_modules" | quote }}
          volumeMounts:
            - name: flags
              mountPath: {{ .Values.gateway.flagsDir | quote }}
          readinessProbe:
            httpGet:
              path: {{ .Values.gateway.healthPath }}
              port: {{ .Values.gateway.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.gateway.healthPath }}
              port: {{ .Values.gateway.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            {{- toYaml .Values.gateway.resources | nindent 12 }}
        {{- if .Values.nullfield.enabled }}
        - name: nullfield
          image: "{{ .Values.nullfield.image }}:{{ .Values.nullfield.tag }}"
          imagePullPolicy: {{ .Values.nullfield.pullPolicy }}
          ports:
            - name: proxy
              containerPort: {{ .Values.nullfield.listenPort }}
              protocol: TCP
            - name: admin
              containerPort: {{ .Values.nullfield.adminPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: nullfield-config
          {{- if .Values.nullfield.activePolicySource }}
          env:
            # Override the loader path so the bridged active-policy file
            # (mounted at /etc/nullfield-active) replaces the chart-shipped
            # /etc/nullfield/policy.yaml without colliding with the
            # nullfield-tools ConfigMap mount above.
            - name: NULLFIELD_POLICY_PATH
              value: "/etc/nullfield-active/policy.yaml"
          {{- end }}
          volumeMounts:
            - name: nullfield-tools
              mountPath: /etc/nullfield
              readOnly: true
            {{- if .Values.nullfield.activePolicySource }}
            - name: nullfield-active-policy
              mountPath: /etc/nullfield-active
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: admin
            initialDelaySeconds: 3
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: admin
            initialDelaySeconds: 2
            periodSeconds: 5
          resources:
            {{- toYaml .Values.nullfield.resources | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
        {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: brain-gateway
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "camazotz.selectorLabels" "brain-gateway" | nindent 4 }}
    {{- include "camazotz.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "camazotz.selectorLabels" "brain-gateway" | nindent 4 }}
  ports:
    - name: mcp
      port: {{ .Values.gateway.port }}
      {{- if .Values.nullfield.enabled }}
      targetPort: {{ .Values.nullfield.listenPort }}
      {{- else }}
      targetPort: {{ .Values.gateway.port }}
      {{- end }}
      protocol: TCP
    {{- if .Values.nullfield.enabled }}
    - name: nullfield-admin
      port: {{ .Values.nullfield.adminPort }}
      targetPort: {{ .Values.nullfield.adminPort }}
      protocol: TCP
    {{- end }}
